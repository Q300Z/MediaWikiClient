<?xml version="1.0" encoding="UTF-8"?>

<Shell
    x:Class="MediaWikiClient.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MediaWikiClient"
    Shell.FlyoutBehavior="Disabled">
    <TabBar>
        <Tab Title="Recherche" Icon="search.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:SearchPage}"
                Route="SearchPage" />
        </Tab>
        <Tab Title="Historique" Icon="clock.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:HistoryPage}"
                Route="HistoryPage" />
        </Tab>
        <Tab Title="Paramètres" Icon="setting.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:SettingsPage}"
                Route="SettingsPage" />
        </Tab>
    </TabBar>


</Shell>